// Create a function called findAge that takes in the
// animal object and returns the age.
var animal1 = {
    name: "Jeremy",
    age: 24,
    };
  
    var animal2 = {
    name: "Chase",
    age: 15,
    };
  
  function findAge(animal) {
  
    if (animal.name) {
  return animal.age;
    }
  
    else {
  return animal.age;
    }
  }
  
    console.log(findAge(animal1));
  console.log(findAge(animal2));
